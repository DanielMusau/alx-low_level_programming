#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints its name follwed by a new line.
 * @argc: Number of command line arguments.
 * @argv: 1-dimensional array that contains the comand line arguments.
 * Return: 0.
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);
	return (0);
}
